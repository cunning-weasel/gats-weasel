import * as React from 'react';
import Nav from "../componenets/Nav";

const projects = () => {
    return (
        <div>
            <Nav />
            <h1>here I'll do the api fetches and search bar</h1>
        </div>
    )
};

export default projects;
